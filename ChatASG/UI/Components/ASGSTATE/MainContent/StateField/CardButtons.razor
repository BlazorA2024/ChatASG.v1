@using global::Data.MainContent
@if (Prams != null)
{
    <button id="add-state-btn" @onclick="HandleClick" class="@Prams.DataBuild.ClassButton ">
        <i class="@Prams.DataBuild.Icon "></i>@Prams.DataBuild.Button
</button>

}
@code {


    [Parameter] public CardButton? Prams { get; set; }
    [Parameter] public EventCallback<DataButton> OnClick { get; set; }
   
    private async Task HandleClick()
    {
        if (Prams?.DataBuild != null)
        {
            StateHasChanged();
            await OnClick.InvokeAsync(Prams.DataBuild);
        }
    }
   
}
@* <button id="add-state-btn" class="bg-blue-600 hover:bg-blue-700 text-white px-3 py-2 rounded-md transition-all duration-300 transform hover:scale-105">
    <i class="fas fa-plus"></i>
</button> *@