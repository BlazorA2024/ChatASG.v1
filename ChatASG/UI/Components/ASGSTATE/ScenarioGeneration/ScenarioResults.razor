@using global::Data.ScenarioGeneration
@if (Prams != null)
{
    <!-- Scenario Results -->
    <div class="border rounded-md p-4 bg-gray-50 max-h-64 overflow-y-auto">
        <h3 class="text-sm font-medium text-gray-700 mb-3 flex items-center">
            <i class="@Prams.DataBuild.Icon  mr-1 text-gray-500"></i>@Prams.DataBuild.Name 
        </h3>
        <div id="scenario-results" class="space-y-3">
            <div class="text-center py-8 text-gray-500">
                <i class="@Prams.DataBuild.Icons  text-2xl mb-2"></i>
                <p class="text-sm">@Prams.DataBuild.Title</p>
                <p class="text-xs mt-1">@Prams.DataBuild.Description    </p>
            </div>
        </div>
    </div>



}
@code {


    [Parameter] public CardResults? Prams { get; set; }
   
    // protected override async Task OnInitializedAsync()
    // {
    //     await UpdateStyleAsync(CLASSES);
    //     await base.OnInitializedAsync();
    // }
}
@* @using global::Data.Pricing
@inherits StylesCardAddPricing
@if (Prams != null)
{
    <section class="@ClassContainer ">
        <div class="@ClassItem ">
            <h2 class="@ClassName ">@Prams.DataBuild.Name  </h2>
            <p class="@ClassDescription ">
             @Prams.DataBuild.Description   
            </p>
        </div>

        <div class="@ClassItems ">
            @foreach (var plan in Prams.Items)
            {
                <div>
                    <CardPriceng Prams="plan" />
                </div>
            }
        </div>
        <CardPricengQuiz Prams="Prams.Iquiz" />

    </section>
}
@code {

   
    [Parameter] public CardAddPricing? Prams { get; set; }

    protected override async Task OnInitializedAsync()
    {
        await UpdateStyleAsync(CLASSES);
        await base.OnInitializedAsync();
    }
}
 *@