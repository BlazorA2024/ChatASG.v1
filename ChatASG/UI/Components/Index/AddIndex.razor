@using ChatASG.UI.Components.Index.ContactSection
@using ChatASG.UI.Components.Index.FAQ
@using ChatASG.UI.Components.Index.FinalCTA
@using ChatASG.UI.Components.Index.Footer
@using ChatASG.UI.Components.Index.HeroSection
@using ChatASG.UI.Components.Index.Ourservices
@using ChatASG.UI.Components.Index.Pricing
@using ChatASG.UI.Components.Index.ServicesSection
@using ChatASG.UI.Components.Index.Technology
@using ChatASG.UI.Components.Index.FeaturesSection

@using ChatASG.UI.Components.Templates

<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>منصة ASG للأمن السيبراني</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
       
        
        :root {
            --primary: #2563EB;
            --secondary: #1D4ED8;
            --accent: #3B82F6;
            --dark: #0F172A;
            --light: #F8FAFC;
            --success: #10B981;
            --danger: #EF4444;
            --warning: #F59E0B;
            --cyber-blue: #1E40AF;
            --cyber-red: #DC2626;
        }
        
        body {
            font-family: 'Tajawal', sans-serif;
            background-color: #0F172A;
            color: white;
            margin: 0;
        }
        
        .cyber-gradient {
            background: linear-gradient(135deg, var(--cyber-blue) 0%, var(--dark) 100%);
        }
        
        .card-hover {
            transition: all 0.3s ease;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }
        
        .card-hover:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.3);
            border-color: var(--accent);
        }
        
        .nav-link {
            position: relative;
        }
        
        .nav-link::after {
            content: '';
            position: absolute;
            width: 0;
            height: 2px;
            bottom: -2px;
            right: 0;
            background-color: var(--accent);
            transition: width 0.3s ease;
        }
        
        .nav-link:hover::after {
            width: 100%;
        }
        
        .feature-icon {
            width: 60px;
            height: 60px;
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: 12px;
            margin-bottom: 20px;
        }
        
        .cyber-pattern {
            background-image: radial-gradient(circle, rgba(30, 64, 175, 0.1) 1px, transparent 1px);
            background-size: 20px 20px;
        }
        
        .animated-dots::after {
            content: '...';
            animation: dotAnimation 1.5s infinite;
        }
        
        
        .cyber-border {
            position: relative;
        }
        
        .cyber-border::before {
            content: '';
            position: absolute;
            inset: 0;
            border-radius: inherit;
            padding: 1px;
            background: linear-gradient(135deg, var(--accent), var(--cyber-red));
            -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
            -webkit-mask-composite: xor;
            mask-composite: exclude;
            pointer-events: none;
        }
    </style>
</head>


<body class="cyber-pattern">
    
    <!-- Hero Section -->
     <SectionTemplate  /> 
@*     <HeroIntroList />
 *@   

    <!-- Features Section -->
     <ChatASG.UI.Components.Index.FeaturesSection.FeaturesSectionTemplate /> 
@*     <FeatureItemOut />
 *@
    <br/>
    <br/>
    <!-- Explore of Studios -->
<!-- 3 استكشاف الاستوديوهات    -->
    <ServicesSectionTemplate />
 
@*        <Studios />
 *@    <!--4 Services Section -->
    <!-- خدماتنا المتكاملة    -->
         <OurservicesTemplate />
     @*  <TOurservices /> *@

    <!-- 5  -->

 <TechnologyTemplate />

    <!-- Pricing -->
@*     <PricingTemplate />
 *@
    <!-- FAQ -->
    <FaqTemplate />


    <!-- Final CTA -->
    <FinalCTATemplate />
   <!-- Contact Section -->
  <ContactSectionTemplate   />

    <!-- Footer -->
    <FooterTemplate />
   
    <!-- Back to Top Button -->
    <button id="back-to-top" class="fixed bottom-8 right-8 bg-blue-600 hover:bg-blue-700 text-white w-12 h-12 rounded-full flex items-center justify-center shadow-lg transition duration-300 opacity-0 invisible">
        <i class="fas fa-arrow-up"></i>
    </button>

    <script>
        // Mobile menu toggle
        document.getElementById('mobile-menu-button').addEventListener('click', function() {
            const menu = document.getElementById('mobile-menu');
            menu.classList.toggle('hidden');
        });
        
        // Back to top button
        const backToTopButton = document.getElementById('back-to-top');
        
        window.addEventListener('scroll', function() {
            if (window.pageYOffset > 300) {
                backToTopButton.classList.remove('opacity-0', 'invisible');
                backToTopButton.classList.add('opacity-100', 'visible');
            } else {
                backToTopButton.classList.remove('opacity-100', 'visible');
                backToTopButton.classList.add('opacity-0', 'invisible');
            }
        });
        
        backToTopButton.addEventListener('click', function() {
            window.scrollTo({
                top: 0,
                behavior: 'smooth'
            });
        });
        
        // Smooth scrolling for navigation links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function(e) {
                e.preventDefault();
                
                const targetId = this.getAttribute('href');
                if (targetId === '#') return;
                
                const targetElement = document.querySelector(targetId);
                if (targetElement) {
                    window.scrollTo({
                        top: targetElement.offsetTop - 80,
                        behavior: 'smooth'
                    });
                    
                    // Close mobile menu if open
                    const mobileMenu = document.getElementById('mobile-menu');
                    if (!mobileMenu.classList.contains('hidden')) {
                        mobileMenu.classList.add('hidden');
                    }
                }
            });
        });
    </script>
</body>
</html>